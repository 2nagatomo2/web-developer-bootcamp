<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>キャンプ場の編集</title>
  </head>
  <body>
    <h1>キャンプ場の編集</h1>
    <form
      action="/campgrounds/<%= campground._id %>?_method=PATCH"
      　
      method="POST"
    >
      <section>
        <label for="title">キャンプ場名</label
        ><input
          type="text"
          name="title"
          id="title"
          placeholder="キャンプ場名"
          value="<%= campground.title %>"
        />
        <label for="price">価格</label
        ><input
          type="number"
          name="price"
          id="price"
          placeholder="価格"
          value="<%= campground.price %>"
        />
        <label for="location">住所</label
        ><input
          type="text"
          name="location"
          id="location"
          placeholder="住所"
          value="<%= campground.location %>"
        />
      </section>
      <br />
      <label for="description">説明</label
      ><textarea name="description" id="description" placeholder="説明">
        <%= campground.description %>
    </textarea
      >
      <br />
      <button>編集</button>
    </form>
    <p>
      <a href="/campgrounds/<%= campground._id %>">キャンセル</a>
    </p>
    <footer>
      <a href="/campgrounds">一覧に戻る</a>
    </footer>
  </body>
</html>
